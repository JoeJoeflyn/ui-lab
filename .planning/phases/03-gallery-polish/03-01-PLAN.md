# Plan 03-01: Convert all placeholder cards to composite previews + final QA

## Objective
Ensure every effect card is a live composite preview, hero auto-cycles, and no console errors or WebGL leaks remain.

## Tasks

### Task 1: Hero auto-advance timer
- Add a `useEffect` in `EffectHero` that sets a 5-second interval calling `next`
- Clear the interval on unmount and when the user manually navigates (reset timer on click)
- Pause auto-advance when the user hovers over the hero (optional — keeps it simple: just reset on manual interaction)

### Task 2: Hero cycles through ALL effects
- Change `page.tsx` to pass `ALL_EFFECTS` to `EffectHero` instead of `HOVER_EFFECTS`
- The hero already filters by `implemented` and caps dots at 12, so this will work with 125 effects

### Task 3: TypeScript + Build verification
- Run `npx tsc --noEmit`
- Run `npm run build`
- Fix any warnings or errors

### Task 4: Final verification
- Verify no "coming soon" badges (all 125 effects implemented)
- Verify hero auto-cycles
- Verify WebGL cleanup is correct
- Verify copy button works for both hover and entrance
- Update STATE.md to mark Phase 3 complete

## Files to modify
- `src/components/effect-cards.tsx` — add auto-advance timer to EffectHero
- `src/app/page.tsx` — pass ALL_EFFECTS to hero
- `.planning/STATE.md` — mark Phase 3 complete
